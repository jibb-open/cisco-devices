function reactToEmailEdit() {
  xapi.Event.UserInterface.Extensions.Widget.Action.on((event) => {
    if (event.Type === "released" && event.WidgetId === "jibbemail") {

      xapi.Command.UserInterface.Message.TextInput.Display({
        FeedbackId: "useremail",
        Title: "Set your Auto-Save Email",
        Text: "JIBB will send your PDF automatically. If empty, the meeting host will receive it.",
        Placeholder: RecordingEmail || "Enter email address",
      });

    }
  });
}

function setEmailistener() {
  xapi.Event.UserInterface.Message.TextInput.Response.on(async (event) => {
    if (event.FeedbackId === "useremail") {
      RecordingEmail = event.Text.trim();
    }
  });
}